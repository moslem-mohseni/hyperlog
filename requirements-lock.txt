# HyLog locked production requirements.
#
# This file is the canonical "reproduce-exactly" anchor for Phase 7. The
# version pins below are the ones the CI green-light, the Docker image,
# and the reproduce_all scripts all install.
#
# Regenerate (only on a verified-clean reproduction machine):
#   python -m pip install -e ".[dev]"
#   python -m pip freeze --exclude-editable > requirements-lock.txt
#
# The pinned versions intentionally allow patch-level upgrades within the
# same minor version so security backports flow in automatically. They
# do NOT allow minor version drift — that would require re-running the
# Phase 6 ablation matrix.

# Core ML stack.
torch==2.4.1
transformers==4.45.2
peft==0.12.0
accelerate==0.34.2
bitsandbytes==0.43.3; platform_system != "Darwin"
datasets==2.20.0
tokenizers==0.20.0
safetensors==0.4.5
huggingface-hub==0.25.2

# Config + experiment tracking.
hydra-core==1.3.2
omegaconf==2.3.0
mlflow==2.16.2

# Numerical / data.
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2

# CLI + serving.
click==8.1.7
fastapi==0.115.0
uvicorn==0.31.0
pydantic==2.8.2
tqdm==4.66.5
rich==13.7.1

# Configuration / formats.
pyyaml==6.0.2

# Optional plotting (matplotlib) — used by reliability + curves PNG
# emission only. Loaded lazily; missing matplotlib does not break tests.
matplotlib==3.9.2

# Dev tooling (kept here for the reproduce_all checklist; production
# images omit them).
pytest==8.3.3
pytest-cov==5.0.0
pytest-xdist==3.6.1
hypothesis==6.112.2
ruff==0.6.9
mypy==1.11.2
pre-commit==3.8.0
types-PyYAML==6.0.12.20240917
