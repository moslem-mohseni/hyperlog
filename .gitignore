# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
*.egg
.installed.cfg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
coverage.xml
*.cover
nosetests.xml

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
.python-version

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Project-specific: model and experiment artefacts
data/raw/
data/processed/
data/cache/
models/checkpoints/
mlruns/
mlartifacts/
wandb/
outputs/
multirun/
*.ckpt
*.pt
*.bin
*.safetensors
*.onnx

# Logs
*.log
logs/
# …but test fixtures are tracked even if they end in .log.
!tests/fixtures/**/*.log

# Notebooks
.ipynb_checkpoints/

# Local config overrides
configs/local/
*.local.yaml
*.local.yml

# Initial materials (preserved but not in source path)
# docs/00_initial_materials/ IS tracked — these are the project's seed documents.
.release_*.json

# Per-run LOSO / calibration artefacts (regenerated by the CLIs).
# The directories themselves are preserved as markers but their
# contents are run-specific.
reports/phase4/runs/
reports/phase5/runs/
reports/phase6/runs/

# Upstream sources vendored for parity inspection only (Phase 2A).
# Reclonable via scripts/feasibility_check.{ps1,sh}.
third_party/
